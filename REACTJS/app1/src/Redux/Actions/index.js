const defaultState = {
    user: []
}
export default function rootReducer(state = defaultState, action) {
    switch (action.type) {
        case "ADD_USER":

        case "CREATE_USER":

        case "UPDATE_USER":

        default:
            return state
    }
}